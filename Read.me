1. npm init 
// to start, use "node src/app.js" or otw mention in script 
2. npm i express --save
// express needs to be required
3. npm i -g nodemon
//error handling using app.use("/". (Err, req, res, next) = {});
4. npm install dotenv
// you need to require mongoose too (use camel casing)
//how to use a middleware - you use a app.use
//take help of mongoose documentation when needed