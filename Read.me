1. npm init 
// to start, use "node src/app.js" or otw mention in script 
2. npm i express --save
// express needs to be required
3. npm i -g nodemon
//error handling using app.use("/". (Err, req, res, next) = {});
4. npm install dotenv
// you need to require mongoose too (use camel casing)
//how to use a middleware - you use a app.use
//take help of mongoose documentation when neede
- ADD schema lvl validation and API level validdation
5. npm i  validator
//validator needs to be required like express
//never trust your req.body
//validator documentation refer
for post signup :
  a. Validation of data
  b. Encrypt the password
  c. Creating a new instance of the user model
6. npm i bcrypt  